# i18n 多语言支持实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 TBH Companion 添加 4 语言（en/zh-CN/ja/ko）UI 与主进程通知/对话框的完整 i18n 支持，使用 i18next + react-i18next，零新增 IPC 通道。

**Architecture:** 翻译 JSON 放 `app/shared/locales/{lang}/{ns}.json`；纯工厂 `createI18n()` 在 `app/src/core/i18n/factory.ts`；主进程与渲染进程各自懒初始化独立 i18next 实例；语言通过 `AppConfig.language` 字段经现有 `SAVE_CONFIG` 通道流动，`applyConfigPatch` 接入语言切换副作用（重建托盘菜单 + 同步主进程 i18n）。

**Tech Stack:** TypeScript, i18next ^25, react-i18next ^15, Electron, React 19, Vitest

**Spec:** [docs/superpowers/specs/2026-07-19-i18n-design.md](../specs/2026-07-19-i18n-design.md)

---

## 文件结构

### 新建文件

| 文件 | 职责 |
|------|------|
| `app/shared/language.ts` | `AppLanguage` 类型、`APP_LANGUAGES` 常量、`resolveLanguage()` 纯函数 |
| `app/shared/locales/en/{15 个 ns}.json` | 英文源翻译 |
| `app/shared/locales/zh-CN/{15 个 ns}.json` | 简体中文翻译（Phase 3 填充） |
| `app/shared/locales/ja/{15 个 ns}.json` | 日文翻译（Phase 4 填充） |
| `app/shared/locales/ko/{15 个 ns}.json` | 韩文翻译（Phase 4 填充） |
| `app/src/core/i18n/factory.ts` | 纯 `createI18n()` 工厂 + `ResolvedLanguage` 类型 |
| `app/src/main/i18n.ts` | 主进程 i18next 懒初始化、`t()`、`changeLanguage()` |
| `app/src/renderer/i18n.ts` | 渲染进程 i18next + react-i18next 初始化 |
| `app/test/core/i18n/factory.test.ts` | 工厂单元测试 |
| `app/test/main/i18n.test.ts` | 主进程 i18n 测试 |
| `app/test/shared/language.test.ts` | `resolveLanguage` 测试 |
| `app/test/renderer-component/i18n.test.tsx` | 渲染进程 i18n 集成测试 |

### 修改文件

| 文件 | 改动 |
|------|------|
| `app/package.json` | +i18next, +react-i18next |
| `app/shared/types.ts` | `AppConfig` 追加 `language: AppLanguage` |
| `app/src/main/config.ts` | `DEFAULTS.language="auto"`、`sanitizeLanguage()`、`normalizeConfig` 接入 |
| `app/src/main/index.ts` | `whenReady` 内调用 `initMainI18n(config)`、错误框用 `t()` |
| `app/src/main/tray/trayService.ts` | 5 个 label 用 `t()`、新增 `rebuildTrayMenu()` |
| `app/src/main/services/NotificationService.ts` | 构造函数注入 `t`，4 条通知文本用 `t()` |
| `app/src/main/app/appState.ts` | 对话框 title 用 `t()`、注入 `onLanguageChanged` |
| `app/src/main/ipc/configPatch.ts` | `applyConfigPatch` 末尾处理 `patch.language` |
| `app/src/renderer/Root.tsx` | 无需改（i18nReady gate 在 TbhProvider 内） |
| `app/src/renderer/context/TbhProvider.tsx` | 挂载时初始化 i18n，ready gate 包裹 children |
| `app/src/renderer/components/appTabs.ts` | `getVisibleTabs` 只返回 `TabId[]`，去掉 label |
| `app/src/renderer/components/AppTabBar.tsx` | 用 `t(\`tabs.\${id}\`)` 渲染 label |
| `app/src/renderer/tabs/Settings.tsx` | 新增 Language 选择器；其余文本 Phase 2 改造 |
| `app/src/renderer/tabs/About.tsx` | 全部文本走 `t()` |
| `app/test/main/config.test.ts` | 补充 `language` 字段 sanitize 测试 |
| `app/test/main/configPatch.test.ts` | 补充 `patch.language` 触发 `onLanguageChanged` 测试 |
| `app/test/main/notificationService.test.ts` | 适配 `t` 注入 |

---

## Phase 1：基础设施 + 高频表面

### Task 1.1: 添加依赖

**Files:**
- Modify: `app/package.json`

- [ ] **Step 1: 安装 i18next 与 react-i18next**

Run（在 `app/` 目录下）:
```powershell
pnpm add i18next@^25 react-i18next@^15
```

预期：`package.json` 的 `dependencies` 新增两行；`pnpm-lock.yaml` 更新。

- [ ] **Step 2: 验证 typecheck 通过**

Run: `pnpm typecheck`
预期：PASS（无新增错误）

- [ ] **Step 3: Commit**

```bash
git add app/package.json pnpm-lock.yaml
git commit -m "chore(i18n): add i18next and react-i18next dependencies"
```

---

### Task 1.2: 新建语言类型与 resolveLanguage 纯函数

**Files:**
- Create: `app/shared/language.ts`
- Test: `app/test/shared/language.test.ts`

- [ ] **Step 1: 写失败的测试**

`app/test/shared/language.test.ts`:
```typescript
import { describe, expect, it } from "vitest";
import {
  APP_LANGUAGES,
  DEFAULT_LANGUAGE,
  resolveLanguage,
} from "../../shared/language";

describe("language", () => {
  it("APP_LANGUAGES contains en, zh-CN, ja, ko", () => {
    expect(APP_LANGUAGES).toEqual(["en", "zh-CN", "ja", "ko"]);
  });

  it("DEFAULT_LANGUAGE is auto", () => {
    expect(DEFAULT_LANGUAGE).toBe("auto");
  });

  it("resolveLanguage returns explicit language unchanged", () => {
    expect(resolveLanguage("en", "zh-CN")).toBe("en");
    expect(resolveLanguage("zh-CN", "en-US")).toBe("zh-CN");
    expect(resolveLanguage("ja", "en-US")).toBe("ja");
    expect(resolveLanguage("ko", "en-US")).toBe("ko");
  });

  it("resolveLanguage maps auto to system locale", () => {
    expect(resolveLanguage("auto", "zh-CN")).toBe("zh-CN");
    expect(resolveLanguage("auto", "zh-TW")).toBe("zh-CN");
    expect(resolveLanguage("auto", "ja-JP")).toBe("ja");
    expect(resolveLanguage("auto", "ko-KR")).toBe("ko");
    expect(resolveLanguage("auto", "en-US")).toBe("en");
  });

  it("resolveLanguage falls back to en for unknown locale", () => {
    expect(resolveLanguage("auto", "fr-FR")).toBe("en");
    expect(resolveLanguage("auto", "de-DE")).toBe("en");
  });

  it("resolveLanguage is case-insensitive on locale", () => {
    expect(resolveLanguage("auto", "ZH-cn")).toBe("zh-CN");
    expect(resolveLanguage("auto", "JA-jp")).toBe("ja");
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test -- app/test/shared/language.test.ts`
预期：FAIL，模块不存在。

- [ ] **Step 3: 实现 `app/shared/language.ts`**

```typescript
// UI 语言类型与解析工具。shared 层 — 为主进程、渲染进程、core 共享。

/** 已支持的具体语言（不含 "auto"）。 */
export const APP_LANGUAGES = ["en", "zh-CN", "ja", "ko"] as const;
export type AppLanguage = (typeof APP_LANGUAGES)[number] | "auto";

export const DEFAULT_LANGUAGE: AppLanguage = "auto";

/** resolveLanguage 的返回类型：去掉 "auto" 的具体语言。 */
export type ResolvedLanguage = (typeof APP_LANGUAGES)[number];

/**
 * 将配置中的 language（可能是 "auto"）解析为具体 BCP-47 标签。
 * "auto" 时按 systemLocale 前缀匹配；未知 locale 回退 "en"。
 */
export function resolveLanguage(
  language: AppLanguage,
  systemLocale: string,
): ResolvedLanguage {
  if (language !== "auto") return language;
  const lower = systemLocale.toLowerCase();
  if (lower.startsWith("zh")) return "zh-CN";
  if (lower.startsWith("ja")) return "ja";
  if (lower.startsWith("ko")) return "ko";
  return "en";
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm test -- app/test/shared/language.test.ts`
预期：PASS（6 个测试全过）

- [ ] **Step 5: Commit**

```bash
git add app/shared/language.ts app/test/shared/language.test.ts
git commit -m "feat(i18n): add AppLanguage type and resolveLanguage helper"
```

---

### Task 1.3: 新建翻译 JSON 骨架（en 完整 + 其他语言占位）

**Files:**
- Create: `app/shared/locales/en/{15 个 ns}.json`
- Create: `app/shared/locales/zh-CN/{15 个 ns}.json`（均为 `{}`）
- Create: `app/shared/locales/ja/{15 个 ns}.json`（均为 `{}`）
- Create: `app/shared/locales/ko/{15 个 ns}.json`（均为 `{}`）

- [ ] **Step 1: 创建 en/tabs.json**

`app/shared/locales/en/tabs.json`:
```json
{
  "live": "Live",
  "inventory": "Inventory",
  "chests": "Chests",
  "loot": "Loot",
  "pets": "Pets",
  "lookup": "Lookup",
  "market": "Market",
  "settings": "Settings",
  "about": "About",
  "debug": "Debug"
}
```

- [ ] **Step 2: 创建 en/tray.json**

`app/shared/locales/en/tray.json`:
```json
{
  "tooltip": "TBH Companion",
  "show": "Show",
  "miniOverlay": "Mini overlay",
  "boxTracker": "Stage boss chest tracker",
  "quit": "Quit"
}
```

- [ ] **Step 3: 创建 en/notifications.json**

`app/shared/locales/en/notifications.json`:
```json
{
  "updateAvailableTitle": "Update available",
  "updateAvailableBody": "TBH Companion v{{version}} is available. Open About to download.",
  "inventoryAlmostFullTitle": "Inventory almost full",
  "inventoryAlmostFullBody": "{{used}}/{{capacity}} slots used ({{percent}}%).",
  "slots_one": "{{count}} slot used",
  "slots_other": "{{count}} slots used"
}
```

- [ ] **Step 4: 创建 en/dialogs.json**

`app/shared/locales/en/dialogs.json`:
```json
{
  "chooseSaveFile": "Choose TBH save file",
  "saveFileFilter": "TBH save",
  "fatalErrorTitle": "TBH Companion — startup failed",
  "fatalErrorBody": "The item catalog could not be loaded. Reinstall or update the app.\n\n{{detail}}"
}
```

- [ ] **Step 5: 创建 en/about.json**

`app/shared/locales/en/about.json`:
```json
{
  "title": "About",
  "updateDisabled": "Updates aren't available in this build.",
  "updateChecking": "Checking for a newer release…",
  "updateNotAvailable": "You're on the latest release.",
  "updateAvailableWithVersion": "Version {{version}} is available.",
  "updateAvailableGeneric": "A newer version is available.",
  "updateDownloading": "Downloading update…",
  "updateReadyWithVersion": "Version {{version}} is ready to install.",
  "updateReadyGeneric": "Update downloaded and ready to install.",
  "updateErrorFallback": "Update check failed.",
  "checkForUpdates": "Check for updates",
  "downloadUpdate": "Download update",
  "restartToInstall": "Restart to install",
  "githubReleases": "GitHub releases"
}
```

- [ ] **Step 6: 创建 en/settings.json（仅语言选择器部分；其余 Phase 2 补全）**

`app/shared/locales/en/settings.json`:
```json
{
  "language": {
    "label": "Language",
    "auto": "Follow system",
    "restartHint": "Other windows will pick up the new language on restart."
  }
}
```

- [ ] **Step 7: 创建 en/common.json（占位，Phase 2 补全）**

`app/shared/locales/en/common.json`:
```json
{
  "clear": "Clear",
  "clearing": "Clearing…",
  "save": "Save",
  "saving": "Saving…",
  "cancel": "Cancel",
  "ok": "OK",
  "yes": "Yes",
  "no": "No",
  "refresh": "Refresh",
  "loading": "Loading…",
  "error": "Error",
  "retry": "Retry"
}
```

- [ ] **Step 8: 创建 en/ 其余 8 个 namespace 占位文件**

为以下文件创建空 `{}`：
- `app/shared/locales/en/live.json`
- `app/shared/locales/en/inventory.json`
- `app/shared/locales/en/market.json`
- `app/shared/locales/en/loot.json`
- `app/shared/locales/en/chests.json`
- `app/shared/locales/en/lookup.json`
- `app/shared/locales/en/pets.json`
- `app/shared/locales/en/liveMemory.json`

- [ ] **Step 9: 创建 zh-CN / ja / ko 各 15 个空 JSON 文件**

每个文件内容均为 `{}`。共 45 个文件。

- [ ] **Step 10: 验证 typecheck 通过**

Run: `pnpm typecheck`
预期：PASS

- [ ] **Step 11: Commit**

```bash
git add app/shared/locales/
git commit -m "feat(i18n): add locale JSON skeleton for en/zh-CN/ja/ko"
```

---

### Task 1.4: 新建 core/i18n 工厂

**Files:**
- Create: `app/src/core/i18n/factory.ts`
- Test: `app/test/core/i18n/factory.test.ts`

- [ ] **Step 1: 写失败的测试**

`app/test/core/i18n/factory.test.ts`:
```typescript
import { describe, expect, it } from "vitest";
import { createI18n, type ResolvedLanguage } from "../../../src/core/i18n/factory";

const resources: Record<ResolvedLanguage, Record<string, object>> = {
  en: {
    common: { greeting: "Hello", farewell: "Goodbye" },
    tabs: { live: "Live" },
  },
  "zh-CN": {
    common: { greeting: "你好" },
    tabs: { live: "实时" },
  },
  ja: { common: {}, tabs: {} },
  ko: { common: {}, tabs: {} },
};

describe("createI18n", () => {
  it("returns a usable i18next instance", () => {
    const i = createI18n({ language: "en", fallback: "en", resources });
    expect(i.t("common:greeting")).toBe("Hello");
  });

  it("falls back to fallbackLng when key missing in current language", () => {
    const i = createI18n({ language: "zh-CN", fallback: "en", resources });
    // zh-CN has greeting but not farewell
    expect(i.t("common:greeting")).toBe("你好");
    expect(i.t("common:farewell")).toBe("Goodbye");
  });

  it("changeLanguage updates t() output", () => {
    const i = createI18n({ language: "en", fallback: "en", resources });
    expect(i.t("common:greeting")).toBe("Hello");
    void i.changeLanguage("zh-CN");
    expect(i.t("common:greeting")).toBe("你好");
  });

  it("returns key when translation entirely missing", () => {
    const i = createI18n({ language: "en", fallback: "en", resources });
    expect(i.t("common:nonexistent")).toBe("common:nonexistent");
  });

  it("interpolates variables", () => {
    const i = createI18n({
      language: "en",
      fallback: "en",
      resources: {
        ...resources,
        en: {
          ...resources.en,
          common: { ...resources.en.common, welcome: "Hi {{name}}" },
        },
      },
    });
    expect(i.t("common:welcome", { name: "Alice" })).toBe("Hi Alice");
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test -- app/test/core/i18n/factory.test.ts`
预期：FAIL（模块不存在）

- [ ] **Step 3: 实现 `app/src/core/i18n/factory.ts`**

```typescript
// 纯 i18next 工厂：不依赖 electron / react，可被 main 与 renderer 复用。
// 调用方必须先用 resolveLanguage() 把 "auto" 解析为具体语言。

import i18next, { type i18n as I18nInstance } from "i18next";
import type { ResolvedLanguage } from "../../../shared/language";

export type { ResolvedLanguage };

export interface CreateI18nOptions {
  language: ResolvedLanguage;
  fallback: ResolvedLanguage;
  resources: Record<ResolvedLanguage, Record<string, object>>;
}

const NAMESPACES = [
  "common", "tabs", "settings", "live", "inventory", "market",
  "loot", "chests", "lookup", "pets", "about", "liveMemory",
  "tray", "notifications", "dialogs",
] as const;

/** 创建并初始化一个独立的 i18next 实例（同步初始化，资源内联）。 */
export function createI18n(opts: CreateI18nOptions): I18nInstance {
  const instance = i18next.createInstance();
  instance.init({
    lng: opts.language,
    fallbackLng: opts.fallback,
    resources: opts.resources,
    defaultNS: "common",
    ns: [...NAMESPACES],
    interpolation: { escapeValue: false },
    returnEmptyString: false,
  });
  return instance;
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm test -- app/test/core/i18n/factory.test.ts`
预期：PASS（5 个测试全过）

- [ ] **Step 5: Commit**

```bash
git add app/src/core/i18n/factory.ts app/test/core/i18n/factory.test.ts
git commit -m "feat(i18n): add pure createI18n factory in core/"
```

---

### Task 1.5: AppConfig 扩展 language 字段

**Files:**
- Modify: `app/shared/types.ts`（AppConfig 接口）
- Modify: `app/src/main/config.ts`（DEFAULTS + sanitizeLanguage + normalizeConfig）
- Test: `app/test/main/config.test.ts`

- [ ] **Step 1: 阅读现有 config.test.ts 了解测试模式**

Run: `pnpm test -- app/test/main/config.test.ts`
预期：现有测试 PASS（确认基线）

- [ ] **Step 2: 在 config.test.ts 添加失败测试**

在 `app/test/main/config.test.ts` 末尾追加：
```typescript
import { APP_LANGUAGES, DEFAULT_LANGUAGE } from "../../shared/language";

describe("config language", () => {
  it("defaults to 'auto'", () => {
    const cfg = normalizeConfigFromRaw({});
    expect(cfg.language).toBe(DEFAULT_LANGUAGE);
  });

  it("preserves explicit valid language", () => {
    expect(normalizeConfigFromRaw({ language: "zh-CN" }).language).toBe("zh-CN");
    expect(normalizeConfigFromRaw({ language: "ja" }).language).toBe("ja");
    expect(normalizeConfigFromRaw({ language: "ko" }).language).toBe("ko");
    expect(normalizeConfigFromRaw({ language: "en" }).language).toBe("en");
  });

  it("falls back to 'auto' for invalid value", () => {
    expect(normalizeConfigFromRaw({ language: "fr" }).language).toBe(DEFAULT_LANGUAGE);
    expect(normalizeConfigFromRaw({ language: 123 }).language).toBe(DEFAULT_LANGUAGE);
    expect(normalizeConfigFromRaw({ language: null }).language).toBe(DEFAULT_LANGUAGE);
  });

  it("APP_LANGUAGES covers all supported codes", () => {
    expect(APP_LANGUAGES).toEqual(["en", "zh-CN", "ja", "ko"]);
  });
});
```

注：测试文件顶部应已 import `normalizeConfigFromRaw`；若未 import，添加 import。

- [ ] **Step 3: 运行测试确认失败**

Run: `pnpm test -- app/test/main/config.test.ts`
预期：FAIL（`cfg.language` undefined）

- [ ] **Step 4: 修改 `app/shared/types.ts` 的 AppConfig**

在 `app/shared/types.ts` 顶部 import 处追加：
```typescript
import type { AppLanguage } from "./language";
```

在 `AppConfig` 接口末尾（`inventoryTable?: InventoryTablePrefs;` 之后）追加：
```typescript
  /** UI 语言；"auto" 跟随系统。默认 "auto"。 */
  language: AppLanguage;
```

- [ ] **Step 5: 修改 `app/src/main/config.ts`**

在 import 区追加：
```typescript
import {
  APP_LANGUAGES,
  DEFAULT_LANGUAGE,
  type AppLanguage,
} from "../../shared/language";
```

在 `DEFAULTS` 对象末尾追加：
```typescript
  language: DEFAULT_LANGUAGE,
```

在 `sanitizeMarketLowValueThresholdUsd` 函数下方追加：
```typescript
function sanitizeLanguage(raw: unknown): AppLanguage {
  if (typeof raw !== "string") return DEFAULT_LANGUAGE;
  if (raw === "auto") return "auto";
  if ((APP_LANGUAGES as readonly string[]).includes(raw)) return raw as AppLanguage;
  return DEFAULT_LANGUAGE;
}
```

在 `normalizeConfig` 函数的解构追加 `language: _language`：
```typescript
function normalizeConfig(raw: RawConfig): AppConfig {
  const {
    chestSoundVariant: _legacy,
    // ... existing ...
    lootRingSeconds: _ring,
    language: _language,
  } = raw;
  // ... existing sanitizers ...
  const language = sanitizeLanguage(raw.language);
  return {
    ...DEFAULTS,
    ...rest,
    // ... existing fields ...
    lootRingSeconds,
    language,
  };
}
```

- [ ] **Step 6: 运行测试确认通过**

Run: `pnpm test -- app/test/main/config.test.ts`
预期：PASS（含新增 4 个测试）

- [ ] **Step 7: 运行 typecheck**

Run: `pnpm typecheck`
预期：PASS

- [ ] **Step 8: Commit**

```bash
git add app/shared/types.ts app/src/main/config.ts app/test/main/config.test.ts
git commit -m "feat(i18n): add language field to AppConfig with sanitize"
```

---

### Task 1.6: 新建主进程 i18n 模块

**Files:**
- Create: `app/src/main/i18n.ts`
- Test: `app/test/main/i18n.test.ts`

- [ ] **Step 1: 写失败的测试**

`app/test/main/i18n.test.ts`:
```typescript
import { afterEach, describe, expect, it, vi } from "vitest";

// Mock electron 的 app.getLocale()，因为 i18n 模块依赖它
vi.mock("electron", () => ({
  app: { getLocale: () => "en-US" },
}));

// 动态 import 让 mock 生效
const { initMainI18n, t, changeLanguage } = await import("../../src/main/i18n");

describe("main i18n", () => {
  afterEach(() => {
    // 切回 en 以隔离测试
    changeLanguage("en");
  });

  it("t() returns key before init (startup fallback)", () => {
    // 注意：此测试须在 init 之前跑；为隔离，单独写一个 describe
  });

  it("initMainI18n with en resolves t()", () => {
    initMainI18n({ language: "en" } as never);
    expect(t("tabs:live")).toBe("Live");
    expect(t("tray:show")).toBe("Show");
  });

  it("initMainI18n with auto follows system locale", () => {
    initMainI18n({ language: "auto" } as never); // systemLocale = en-US → en
    expect(t("tabs:live")).toBe("Live");
  });

  it("changeLanguage updates t() output", () => {
    initMainI18n({ language: "en" } as never);
    expect(t("tabs:live")).toBe("Live");
    changeLanguage("zh-CN");
    // zh-CN/tabs.json 是空 {} → 回退 en
    expect(t("tabs:live")).toBe("Live");
  });

  it("interpolation works in main process", () => {
    initMainI18n({ language: "en" } as never);
    expect(t("notifications:updateAvailableBody", { version: "1.2.3" })).toBe(
      "TBH Companion v1.2.3 is available. Open About to download.",
    );
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test -- app/test/main/i18n.test.ts`
预期：FAIL（模块不存在）

- [ ] **Step 3: 实现 `app/src/main/i18n.ts`**

```typescript
// 主进程 i18n 实例。懒初始化：在 app.whenReady() 内由 initMainI18n() 首次调用时创建。
// 在此之前 t() 会回退到 key 本身（即英文源字符串），用于启动早期日志。

import { app } from "electron";
import { createI18n, type ResolvedLanguage } from "../core/i18n/factory";
import { resolveLanguage, type AppLanguage } from "../../shared/language";
import * as en from "../../shared/locales/en";
import * as zhCN from "../../shared/locales/zh-CN";
import * as ja from "../../shared/locales/ja";
import * as ko from "../../shared/locales/ko";
import type { AppConfig } from "../../shared/types";

const resources = { en, "zh-CN": zhCN, ja, ko } as const;

let instance: ReturnType<typeof createI18n> | null = null;

export function initMainI18n(config: Pick<AppConfig, "language">): void {
  const resolved = resolveLanguage(config.language, app.getLocale());
  if (!instance) {
    instance = createI18n({ language: resolved, fallback: "en", resources });
  } else {
    void instance.changeLanguage(resolved);
  }
}

export function t(key: string, opts?: Record<string, unknown>): string {
  if (!instance) return key;
  return instance.t(key, opts);
}

export function changeLanguage(lang: AppLanguage): void {
  if (!instance) return;
  const resolved: ResolvedLanguage = resolveLanguage(lang, app.getLocale());
  void instance.changeLanguage(resolved);
}

export function getMainI18n(): ReturnType<typeof createI18n> {
  if (!instance) throw new Error("mainI18n not initialized — call initMainI18n first");
  return instance;
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm test -- app/test/main/i18n.test.ts`
预期：PASS（5 个测试全过）

- [ ] **Step 5: Commit**

```bash
git add app/src/main/i18n.ts app/test/main/i18n.test.ts
git commit -m "feat(i18n): add main process i18n with lazy init"
```

---

### Task 1.7: 主进程启动时初始化 i18n + 错误框改造

**Files:**
- Modify: `app/src/main/index.ts`

- [ ] **Step 1: 修改 `app/src/main/index.ts`**

在 import 区追加：
```typescript
import { initMainI18n, t } from "./i18n";
```

在 `app.whenReady().then(() => {` 块内、`startTracking()` 之前追加：
```typescript
      initMainI18n(config);
```

注：需要拿到 config。检查 `startTracking` 是否返回 config 或在调用前 `loadConfig()`。看现有代码 `startTracking()` 内部调 `loadConfig()`，我们需要在它之前独立调用一次。修改方案：

```typescript
  app.whenReady().then(() => {
    const appLog = createLogger("app");
    try {
      appLog.info(`TBH Companion v${appVersion()} ready`);
      registerAssetProtocolHandler();
      initMainI18n(loadConfig());  // 新增：在 startTracking 前初始化 i18n
      const sessionUi = startTracking();
      // ... 其余不变 ...
```

在 import 区追加 `loadConfig`：
```typescript
import { loadConfig } from "./config";
```

将 `dialog.showErrorBox` 调用改为：
```typescript
      dialog.showErrorBox(
        t("dialogs:fatalErrorTitle"),
        t("dialogs:fatalErrorBody", { detail }),
      );
```

- [ ] **Step 2: typecheck**

Run: `pnpm typecheck`
预期：PASS

- [ ] **Step 3: 运行所有现有测试确认无回归**

Run: `pnpm test`
预期：PASS（无新增失败）

- [ ] **Step 4: Commit**

```bash
git add app/src/main/index.ts
git commit -m "feat(i18n): init main i18n on startup and translate fatal error dialog"
```

---

### Task 1.8: 托盘菜单改造 + rebuildTrayMenu

**Files:**
- Modify: `app/src/main/tray/trayService.ts`
- Test: `app/test/main/trayService.test.ts`（新建）

- [ ] **Step 1: 写失败测试**

`app/test/main/trayService.test.ts`:
```typescript
import { describe, expect, it, vi } from "vitest";

vi.mock("electron", () => ({
  app: { quit: vi.fn(), getPath: () => "/tmp" },
  Menu: { buildFromTemplate: vi.fn((t) => t) },
  Tray: class {
    setToolTip = vi.fn();
    setContextMenu = vi.fn();
    on = vi.fn();
    isDestroyed = () => false;
    destroy = vi.fn();
  },
}));

const { createTray, rebuildTrayMenu } = await import("../../src/main/tray/trayService");
const { initMainI18n, t } = await import("../../src/main/i18n");

describe("trayService i18n", () => {
  it("tray menu uses t() for labels", () => {
    initMainI18n({ language: "en" } as never);
    const services = {
      showMain: vi.fn(),
      openOverlay: vi.fn(),
      openBoxTracker: vi.fn(),
    } as never;
    createTray(services);
    expect(t("tray:show")).toBe("Show");
    expect(t("tray:quit")).toBe("Quit");
  });

  it("rebuildTrayMenu exists and is callable", () => {
    const services = {
      showMain: vi.fn(),
      openOverlay: vi.fn(),
      openBoxTracker: vi.fn(),
    } as never;
    expect(() => rebuildTrayMenu(services)).not.toThrow();
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test -- app/test/main/trayService.test.ts`
预期：FAIL（`rebuildTrayMenu` 不存在）

- [ ] **Step 3: 改造 `app/src/main/tray/trayService.ts`**

完整替换文件内容：
```typescript
import { app, Menu, Tray } from "electron";

import type { AppServices } from "../app/appState";
import { trayImage } from "../iconPaths";
import { t } from "../i18n";

let tray: Tray | null = null;
let quitting = false;

export function isAppQuitting(): boolean {
  return quitting;
}

export function setAppQuitting(value = true): void {
  quitting = value;
}

function buildMenuTemplate(services: AppServices) {
  return [
    {
      label: t("tray:show"),
      click: () => services.showMain(),
    },
    {
      label: t("tray:miniOverlay"),
      click: () => services.openOverlay(),
    },
    {
      label: t("tray:boxTracker"),
      click: () => services.openBoxTracker(),
    },
    { type: "separator" as const },
    {
      label: t("tray:quit"),
      click: () => {
        setAppQuitting(true);
        app.quit();
      },
    },
  ];
}

export function createTray(services: AppServices): Tray {
  if (tray && !tray.isDestroyed()) return tray;

  tray = new Tray(trayImage());
  tray.setToolTip(t("tray:tooltip"));
  tray.setContextMenu(Menu.buildFromTemplate(buildMenuTemplate(services)));
  tray.on("double-click", () => {
    services.showMain();
  });

  return tray;
}

/** 语言变更时重建托盘菜单与 tooltip。 */
export function rebuildTrayMenu(services: AppServices): void {
  if (!tray || tray.isDestroyed()) return;
  tray.setToolTip(t("tray:tooltip"));
  tray.setContextMenu(Menu.buildFromTemplate(buildMenuTemplate(services)));
}

export function destroyTray(): void {
  if (tray && !tray.isDestroyed()) {
    tray.destroy();
  }
  tray = null;
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm test -- app/test/main/trayService.test.ts`
预期：PASS

- [ ] **Step 5: Commit**

```bash
git add app/src/main/tray/trayService.ts app/test/main/trayService.test.ts
git commit -m "feat(i18n): tray menu uses t() and supports rebuild on language change"
```

---

### Task 1.9: NotificationService 注入 t

**Files:**
- Modify: `app/src/main/services/NotificationService.ts`
- Modify: `app/src/main/app/appState.ts`（构造 NotificationService 时传入 t）
- Test: `app/test/main/notificationService.test.ts`

- [ ] **Step 1: 阅读现有 notificationService.test.ts**

Run: `pnpm test -- app/test/main/notificationService.test.ts`
预期：PASS（基线）

- [ ] **Step 2: 改造 `app/src/main/services/NotificationService.ts`**

修改 constructor 签名与字段：
```typescript
export class NotificationService {
  private readonly getConfig: () => AppConfig;
  private readonly focusMainWindow: () => void;
  private readonly t: (key: string, opts?: Record<string, unknown>) => string;
  private supported: boolean | null = null;
  private lastNotifiedVersion: string | undefined;

  constructor(
    getConfig: () => AppConfig,
    focusMainWindow: () => void,
    t: (key: string, opts?: Record<string, unknown>) => string,
  ) {
    this.getConfig = getConfig;
    this.focusMainWindow = focusMainWindow;
    this.t = t;
  }
```

将 `showUpdateAvailable` 内的硬编码字符串改为：
```typescript
    const notification = new Notification({
      title: this.t("notifications:updateAvailableTitle"),
      body: this.t("notifications:updateAvailableBody", { version }),
    });
```

将 `showInventoryAlmostFull` 内的硬编码字符串改为：
```typescript
      const notification = new Notification({
        title: this.t("notifications:inventoryAlmostFullTitle"),
        body: this.t("notifications:inventoryAlmostFullBody", {
          used: payload.used,
          capacity: payload.capacity,
          percent,
        }),
      });
```

- [ ] **Step 3: 修改 `app/src/main/app/appState.ts` 中 NotificationService 的实例化**

搜索 `new NotificationService`，将第三个参数传 `t`：
```typescript
import { t } from "../i18n";
// ...
const notifications = new NotificationService(() => config, focusMainWindow, t);
```

- [ ] **Step 4: 修改 `app/test/main/notificationService.test.ts`**

在所有 `new NotificationService(...)` 调用处追加第三个参数。最简方案是传入一个 passthrough 函数：
```typescript
const t = (key: string, opts?: Record<string, unknown>) => {
  // 简单 mock：返回 key 或带插值的 key
  if (!opts) return key;
  let result = key;
  for (const [k, v] of Object.entries(opts)) {
    result = result.replace(`{{${k}}}`, String(v));
  }
  return result;
};
const notifications = new NotificationService(() => mockConfig, () => {}, t);
```

或者改为使用真实 i18n：在测试 setup 中 `initMainI18n({ language: "en" })`，然后 `import { t } from "../../src/main/i18n"`，直接传入 `t`。**推荐使用真实 i18n**，让测试验证翻译键的存在性。

- [ ] **Step 5: 运行测试确认通过**

Run: `pnpm test -- app/test/main/notificationService.test.ts`
预期：PASS

- [ ] **Step 6: Commit**

```bash
git add app/src/main/services/NotificationService.ts app/src/main/app/appState.ts app/test/main/notificationService.test.ts
git commit -m "feat(i18n): inject t() into NotificationService for localized notifications"
```

---

### Task 1.10: applyConfigPatch 接入语言切换副作用

**Files:**
- Modify: `app/src/main/ipc/configPatch.ts`
- Modify: `app/src/main/app/appState.ts`（注入 onLanguageChanged）
- Test: `app/test/main/configPatch.test.ts`

- [ ] **Step 1: 阅读现有 configPatch.test.ts 模式**

Run: `pnpm test -- app/test/main/configPatch.test.ts`
预期：PASS（基线）

- [ ] **Step 2: 在 configPatch.test.ts 末尾追加测试**

```typescript
describe("applyConfigPatch language", () => {
  it("calls onLanguageChanged when language changes", () => {
    const onLanguageChanged = vi.fn();
    const prev = makeBaseConfig({ language: "en" });
    const deps = makeDeps({ onLanguageChanged, prev });
    applyConfigPatch(deps, { language: "zh-CN" });
    expect(onLanguageChanged).toHaveBeenCalledOnce();
  });

  it("does not call onLanguageChanged when language unchanged", () => {
    const onLanguageChanged = vi.fn();
    const prev = makeBaseConfig({ language: "en" });
    const deps = makeDeps({ onLanguageChanged, prev });
    applyConfigPatch(deps, { language: "en" });
    expect(onLanguageChanged).not.toHaveBeenCalled();
  });

  it("does not call onLanguageChanged when patch has no language field", () => {
    const onLanguageChanged = vi.fn();
    const prev = makeBaseConfig({ language: "en" });
    const deps = makeDeps({ onLanguageChanged, prev });
    applyConfigPatch(deps, { currency: "USD" });
    expect(onLanguageChanged).not.toHaveBeenCalled();
  });
});
```

注：`makeBaseConfig` / `makeDeps` 是测试文件已有的辅助函数；如未有，按现有模式新增。如果现有的辅助函数不接收参数覆盖字段，需先扩展它们。

- [ ] **Step 3: 运行测试确认失败**

Run: `pnpm test -- app/test/main/configPatch.test.ts`
预期：FAIL（`onLanguageChanged` 未被调用）

- [ ] **Step 4: 修改 `app/src/main/ipc/configPatch.ts`**

在 import 区追加：
```typescript
import { changeLanguage } from "../i18n";
```

在 `ConfigPatchDeps` 接口追加：
```typescript
  /** 语言变更时重建托盘菜单、刷新主进程相关 UI 文本。 */
  onLanguageChanged?: () => void;
```

在 `applyConfigPatch` 函数末尾（`return { ...next };` 之前）追加：
```typescript
  // 语言切换：同步主进程 i18n + 触发托盘菜单重建
  if (patch.language !== undefined && prev.language !== next.language) {
    changeLanguage(next.language);
    deps.onLanguageChanged?.();
  }
```

- [ ] **Step 5: 修改 `app/src/main/app/appState.ts` 注入 onLanguageChanged**

找到 `saveConfigPatch: (patch) => applyConfigPatch({...})` 调用处，在 deps 对象内追加：
```typescript
          onLanguageChanged: () => {
            rebuildTrayMenu(services);
          },
```

注意：`services` 在该作用域应可见（与 `rebuildTrayMenu` 同作用域）。在 import 区追加：
```typescript
import { rebuildTrayMenu } from "../tray/trayService";
```

但 `appState.ts` 内的 `services` 是构建中的对象，可能尚未完全构造。需确认 `rebuildTrayMenu` 接收的 `services` 类型。如果 `services` 是 `AppServices`，且 `AppServices` 此时未完整，可改为传一个仅含三个回调的最小对象：
```typescript
          onLanguageChanged: () => {
            rebuildTrayMenu({
              showMain: () => showMainWindow(),
              openOverlay: () => openOverlayWindow(),
              openBoxTracker: () => openBoxTrackerWindow(),
            });
          },
```

具体函数名需对照 appState.ts 现有代码确认（查看 `showMain`、`openOverlay`、`openBoxTracker` 的实现）。

- [ ] **Step 6: 运行测试确认通过**

Run: `pnpm test -- app/test/main/configPatch.test.ts`
预期：PASS（含新增 3 个测试）

- [ ] **Step 7: Commit**

```bash
git add app/src/main/ipc/configPatch.ts app/src/main/app/appState.ts app/test/main/configPatch.test.ts
git commit -m "feat(i18n): wire language change side-effect in applyConfigPatch"
```

---

### Task 1.11: 渲染进程 i18n 模块

**Files:**
- Create: `app/src/renderer/i18n.ts`
- Test: `app/test/renderer-component/i18n.test.tsx`

- [ ] **Step 1: 写失败测试**

`app/test/renderer-component/i18n.test.tsx`:
```typescript
import { describe, expect, it, beforeEach } from "vitest";
import { render, screen } from "@testing-library/react";
import { useTranslation } from "react-i18next";
import { initRendererI18n, changeRendererLanguage } from "../../src/renderer/i18n";

function Probe() {
  const { t } = useTranslation(["tabs", "common"]);
  return (
    <div>
      <span data-testid="live">{t("tabs:live")}</span>
      <span data-testid="clear">{t("common:clear")}</span>
    </div>
  );
}

describe("renderer i18n", () => {
  beforeEach(async () => {
    await initRendererI18n("en");
  });

  it("renders English translations", async () => {
    await initRendererI18n("en");
    render(<Probe />);
    expect(screen.getByTestId("live").textContent).toBe("Live");
    expect(screen.getByTestId("clear").textContent).toBe("Clear");
  });

  it("changeRendererLanguage updates the rendered text", async () => {
    await initRendererI18n("en");
    render(<Probe />);
    expect(screen.getByTestId("live").textContent).toBe("Live");
    // zh-CN/tabs.json 是空 {} → 回退 en
    await changeRendererLanguage("zh-CN");
    // useTranslation 不自动重渲染除非订阅 languageChanged
    // react-i18next 默认订阅，应触发重渲染
    expect(screen.getByTestId("live").textContent).toBe("Live"); // fallback
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test:dom -- app/test/renderer-component/i18n.test.tsx`
预期：FAIL（模块不存在）

- [ ] **Step 3: 实现 `app/src/renderer/i18n.ts`**

```typescript
// 渲染进程 i18next + react-i18next 初始化。
// 在 TbhProvider 挂载时由 getConfig() 拿到 language 后调用 initRendererI18n()。

import i18next from "i18next";
import { initReactI18next } from "react-i18next";
import { createI18n, type ResolvedLanguage } from "../core/i18n/factory";
import { resolveLanguage, type AppLanguage } from "../../shared/language";
import * as en from "../../shared/locales/en";
import * as zhCN from "../../shared/locales/zh-CN";
import * as ja from "../../shared/locales/ja";
import * as ko from "../../shared/locales/ko";

const resources = { en, "zh-CN": zhCN, ja, ko } as const;

let initialized = false;

export async function initRendererI18n(language: AppLanguage): Promise<typeof i18next> {
  const resolved: ResolvedLanguage = resolveLanguage(language, navigator.language);
  if (initialized) {
    await i18next.changeLanguage(resolved);
    return i18next;
  }
  const instance = createI18n({ language: resolved, fallback: "en", resources });
  instance.use(initReactI18next);
  await instance.init();
  initialized = true;
  return i18next;
}

export function changeRendererLanguage(language: AppLanguage): Promise<void> {
  const resolved: ResolvedLanguage = resolveLanguage(language, navigator.language);
  return i18next.changeLanguage(resolved);
}

export { i18next };
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm test:dom -- app/test/renderer-component/i18n.test.tsx`
预期：PASS（2 个测试）

- [ ] **Step 5: Commit**

```bash
git add app/src/renderer/i18n.ts app/test/renderer-component/i18n.test.tsx
git commit -m "feat(i18n): add renderer i18n module with react-i18next"
```

---

### Task 1.12: TbhProvider 接入 i18nReady gate

**Files:**
- Modify: `app/src/renderer/context/TbhProvider.tsx`

- [ ] **Step 1: 改造 TbhProvider**

修改 `app/src/renderer/context/TbhProvider.tsx`：

在 import 区追加：
```typescript
import { initRendererI18n } from "../i18n";
```

在组件内 state 区追加：
```typescript
  const [i18nReady, setI18nReady] = useState(false);
```

在现有 `useEffect` 之前新增一个独立的 effect 用于 i18n 初始化：
```typescript
  useEffect(() => {
    let mounted = true;
    void window.tbh
      .getConfig()
      .then((cfg) => initRendererI18n(cfg.language))
      .then(() => {
        if (mounted) setI18nReady(true);
      })
      .catch((err) => {
        reportIpcError(err);
        // 即使失败也要让 UI 渲染（用 fallback en）
        if (mounted) setI18nReady(true);
      });
    return () => {
      mounted = false;
    };
  }, []);
```

在 return 之前加 ready gate：
```typescript
  if (!i18nReady) return null;
```

完整结构示例：
```typescript
export function TbhProvider({ children }: { children: ReactNode }) {
  const [inventory, setInventory] = useState<ResolvedInventory | null>(null);
  const [lastPriceRefreshMessage, setLastPriceRefreshMessage] = useState<string | null>(null);
  const [i18nReady, setI18nReady] = useState(false);
  const { status: catalogStatus, refresh: refreshCatalog } = useCatalogStatus();

  // i18n 初始化（独立 effect，避免与 inventory effect 耦合）
  useEffect(() => {
    let mounted = true;
    void window.tbh
      .getConfig()
      .then((cfg) => initRendererI18n(cfg.language))
      .then(() => mounted && setI18nReady(true))
      .catch((err) => {
        reportIpcError(err);
        if (mounted) setI18nReady(true); // 失败也要继续渲染
      });
    return () => {
      mounted = false;
    };
  }, []);

  // 现有 effect：inventory / notification / prices...
  useEffect(() => { /* ...existing... */ }, []);

  const value = useMemo(/* ...existing... */);

  if (!i18nReady) return null;

  return <TbhContext.Provider value={value}>{children}</TbhContext.Provider>;
}
```

- [ ] **Step 2: typecheck**

Run: `pnpm typecheck`
预期：PASS

- [ ] **Step 3: 运行所有 renderer-component 测试**

Run: `pnpm test:dom`
预期：现有测试可能因 i18n 未初始化而失败 — 需在测试 setup 中初始化 i18n。

- [ ] **Step 4: 修改 `app/test/renderer-component/setup.ts` 添加 i18n 初始化**

读取现有 setup.ts，在末尾追加：
```typescript
import { initRendererI18n } from "../../src/renderer/i18n";

// 全局初始化 i18n，让所有测试组件能用 useTranslation
beforeAll(async () => {
  await initRendererI18n("en");
});
```

如果 setup.ts 已有 `beforeAll`，合并进去。

- [ ] **Step 5: 重新运行 dom 测试**

Run: `pnpm test:dom`
预期：PASS

- [ ] **Step 6: Commit**

```bash
git add app/src/renderer/context/TbhProvider.tsx app/test/renderer-component/setup.ts
git commit -m "feat(i18n): TbhProvider initializes i18n before rendering children"
```

---

### Task 1.13: appTabs + AppTabBar 用 t() 渲染

**Files:**
- Modify: `app/src/renderer/components/appTabs.ts`
- Modify: `app/src/renderer/components/AppTabBar.tsx`
- Test: `app/test/renderer-component/AppTabBar.test.tsx`（已存在）

- [ ] **Step 1: 修改 `app/src/renderer/components/appTabs.ts`**

完整替换为：
```typescript
// Tab 标识符与可见性。label 文本由 AppTabBar 通过 t(`tabs:${id}`) 渲染。

export type TabId =
  | "live"
  | "inventory"
  | "chests"
  | "loot"
  | "pets"
  | "lookup"
  | "market"
  | "settings"
  | "about"
  | "debug";

const TABS: TabId[] = [
  "live",
  "inventory",
  "chests",
  "loot",
  "pets",
  "lookup",
  "market",
  "settings",
  "about",
];

const DEV_TABS: TabId[] = ["debug"];

/** 可见 Tab id 列表 — dev 构建包含 debug tab。 */
export function getVisibleTabs(isDev: boolean): TabId[] {
  return isDev ? [...TABS, ...DEV_TABS] : TABS;
}
```

- [ ] **Step 2: 修改 `app/src/renderer/components/AppTabBar.tsx`**

在 import 区追加：
```typescript
import { useTranslation } from "react-i18next";
```

修改组件：
```typescript
export function AppTabBar({ tab, onTabChange }: { tab: TabId; onTabChange: (tab: TabId) => void }) {
  const { t } = useTranslation("tabs");
  const tabs = getVisibleTabs(import.meta.env.DEV);
  return (
    <div className="flex items-end gap-2 border-b border-border bg-panel px-2 pt-1.5">
      <Tabs
        value={tab}
        onValueChange={(value) => onTabChange(value as TabId)}
        className="min-w-0 flex-1"
      >
        <TabsList
          className="border-b-0 bg-transparent px-0"
          indicatorClassName="transition-none"
          aria-label="Main tabs"
        >
          {tabs.map((id) => (
            <TabsTab key={id} value={id} className="data-[selected]:font-medium">
              {t(`tabs:${id}`)}
            </TabsTab>
          ))}
        </TabsList>
      </Tabs>
      <AppToolbar />
    </div>
  );
}
```

- [ ] **Step 3: 检查并更新 AppTabBar.test.tsx**

Run: `pnpm test:dom -- app/test/renderer-component/AppTabBar.test.tsx`

如果测试断言 Tab 文本是 "Live" / "Inventory" 等，且 i18n 已在 setup.ts 初始化为 en，则测试应继续通过。如果测试用 `getByText("Live")`，PASS。

如果测试用 `getByText("Live")`，PASS。

如果测试断言 label 字段（已不存在），需更新测试。

- [ ] **Step 4: 运行 dom 测试**

Run: `pnpm test:dom -- app/test/renderer-component/AppTabBar.test.tsx`
预期：PASS

- [ ] **Step 5: Commit**

```bash
git add app/src/renderer/components/appTabs.ts app/src/renderer/components/AppTabBar.tsx app/test/renderer-component/AppTabBar.test.tsx
git commit -m "feat(i18n): Tab labels rendered via t('tabs:id')"
```

---

### Task 1.14: Settings Tab 新增 Language 选择器

**Files:**
- Modify: `app/src/renderer/tabs/Settings.tsx`

- [ ] **Step 1: 在 Settings.tsx 顶部添加 useTranslation**

在 import 区追加：
```typescript
import { useTranslation } from "react-i18next";
import { changeRendererLanguage } from "../i18n";
import { APP_LANGUAGES, type AppLanguage } from "../../../shared/language";
```

- [ ] **Step 2: 在 Settings 组件内调用 useTranslation**

在 `function Settings() {` 内第一行追加：
```typescript
  const { t } = useTranslation(["settings", "common"]);
```

- [ ] **Step 3: 找到 General 段（或文件最上方的第一个 Section）**

搜索 Settings.tsx 中的第一个 `<Section>` 块。在其内部最上方追加 Language 选择器：

```tsx
<Field label={t("settings:language.label")}>
  <Select
    value={cfg?.language ?? "auto"}
    disabled={!cfg}
    onChange={(v) => void onLanguageChange(v as AppLanguage)}
    options={[
      { value: "auto", label: t("settings:language.auto") },
      ...APP_LANGUAGES.map((lang) => ({
        value: lang,
        label: LANGUAGE_LABELS[lang],
      })),
    ]}
  />
</Field>
```

并在文件顶部追加常量：
```typescript
const LANGUAGE_LABELS: Record<(typeof APP_LANGUAGES)[number], string> = {
  en: "English",
  "zh-CN": "简体中文",
  ja: "日本語",
  ko: "한국어",
};
```

- [ ] **Step 4: 实现 onLanguageChange**

在 Settings 组件内追加：
```typescript
  async function onLanguageChange(language: AppLanguage): Promise<void> {
    if (!cfg) return;
    try {
      const next = await window.tbh.saveConfig({ language });
      setCfg(next);
      // 同步本窗口的 i18n；其他窗口下次重启同步
      await changeRendererLanguage(next.language);
    } catch (err) {
      reportIpcError(err);
    }
  }
```

- [ ] **Step 5: typecheck**

Run: `pnpm typecheck`
预期：PASS

- [ ] **Step 6: 运行 lint**

Run: `pnpm lint`
预期：PASS（无新增错误）

- [ ] **Step 7: Commit**

```bash
git add app/src/renderer/tabs/Settings.tsx
git commit -m "feat(i18n): add language selector in Settings"
```

---

### Task 1.15: About Tab 完整改造（端到端验证）

**Files:**
- Modify: `app/src/renderer/tabs/About.tsx`

- [ ] **Step 1: 阅读完整 About.tsx**

Run: 用 Read 工具读取 `app/src/renderer/tabs/About.tsx` 全文（约 200 行），列出所有英文字符串。

- [ ] **Step 2: 替换所有英文字符串为 t() 调用**

在 import 区追加：
```typescript
import { useTranslation } from "react-i18next";
```

在组件内：
```typescript
const { t } = useTranslation("about");
```

将 `statusMessage` 函数改为：
```typescript
function statusMessage(status: UpdateStatus): string | null {
  switch (status.phase) {
    case "disabled":
      return t("about:updateDisabled");
    case "checking":
      return t("about:updateChecking");
    case "not-available":
      return t("about:updateNotAvailable");
    case "available":
      return status.availableVersion
        ? t("about:updateAvailableWithVersion", { version: status.availableVersion })
        : t("about:updateAvailableGeneric");
    case "downloading":
      return t("about:updateDownloading");
    case "ready":
      return status.availableVersion
        ? t("about:updateReadyWithVersion", { version: status.availableVersion })
        : t("about:updateReadyGeneric");
    case "error":
      return status.error ?? t("about:updateErrorFallback");
    default:
      return null;
  }
}
```

注意：`statusMessage` 是组件外的函数，无法直接用 `useTranslation`。改为接收 `t` 作为参数，或在组件内 inline。**推荐：移到组件内作为 useCallback。**

将 About.tsx 内其他英文字符串（"Check for updates"、"Download update"、"Restart to install"、"GitHub releases" 等按钮文本）替换为 `t("about:checkForUpdates")` 等。

- [ ] **Step 3: 在 en/about.json 中补全缺失的 key**

如果 Step 2 中发现还有未在 Task 1.3 Step 5 创建的 key，追加到 `app/shared/locales/en/about.json`。

- [ ] **Step 4: typecheck + lint**

Run: `pnpm typecheck && pnpm lint`
预期：PASS

- [ ] **Step 5: 运行 dom 测试**

Run: `pnpm test:dom`
预期：PASS（现有测试不依赖 About 文本）

- [ ] **Step 6: 启动 dev 验证**

Run: `pnpm dev`（在 app/ 目录）
手动验证：
- About 页所有文本正常显示（英文）
- Settings → 切换语言到 zh-CN → About 页未翻译部分回退英文（因为 zh-CN/about.json 是 `{}`）
- 切换回 en → 全部恢复

- [ ] **Step 7: Commit**

```bash
git add app/src/renderer/tabs/About.tsx app/shared/locales/en/about.json
git commit -m "feat(i18n): About tab fully translated via t()"
```

---

### Task 1.16: Phase 1 集成验收

- [ ] **Step 1: 运行完整 QA 套件**

Run: `pnpm qa`
预期：PASS（typecheck + lint + format + test + build + bundle guards 全绿）

如果有失败：
- typecheck 错误：检查 AppConfig / language 类型导入
- lint 错误：未使用的 import / 排序
- test 失败：检查 mock 是否完整
- build 失败：检查 JSON import 在 main + renderer 都工作

- [ ] **Step 2: 手动验收清单**

启动 `pnpm dev`，依次验证：

| 场景 | 预期 |
|------|------|
| 启动后 Tab 标签为英文 | Live / Inventory / Chests / Loot / Pets / Lookup / Market / Settings / About |
| Settings → Language 选 "简体中文" | Tab 标签立即变中文（因 zh-CN/tabs.json 为空，回退 en）→ 实际仍为英文 |
| Settings → Language 选 "English" | Tab 标签英文 |
| Settings → Language 选 "auto"，系统为 zh-CN | Tab 标签英文（回退） |
| 右键托盘图标 | 菜单项为英文（Show / Mini overlay / ...） |
| 触发系统通知（如更新可用） | 通知标题/正文为英文 |
| About 页 | 所有文本英文 |
| 重启应用 | Settings 中 Language 保持上次选择 |
| 触发 fatal error（如删除 catalog） | 错误对话框标题/内容英文 |

- [ ] **Step 3: 修复发现的问题**

如有问题，逐项修复并补充测试。

- [ ] **Step 4: Phase 1 总结提交**

```bash
git add -A
git commit --allow-empty -m "chore(i18n): Phase 1 complete — infrastructure + high-frequency surfaces"
```

---

## Phase 2：主窗口剩余 Tab 翻译改造

Phase 2 是批量改造工作。每个 Tab 按相同模式处理：
1. 阅读现有 tsx，列出所有英文字符串
2. 在 `en/{ns}.json` 添加对应 key
3. 替换 tsx 中的字符串为 `t("{ns}:key")`
4. 运行 typecheck + lint + 该 Tab 的 dom 测试
5. Commit

### Task 2.1: Live.tsx + Overlay.tsx + components/live/*
- [ ] 列出 Live.tsx 所有英文字符串
- [ ] 在 `en/live.json` 添加 key（包括 RATE_TIP / GOLD_TIP / LAP_SECONDS 注释等常量）
- [ ] 改造 `Overlay.tsx`：常量 RATE_TIP / GOLD_TIP 移到 i18n
- [ ] 改造 `Live.tsx`
- [ ] 改造 `components/live/*.tsx`（5 个文件）
- [ ] 运行 typecheck + lint + test:dom
- [ ] Commit

### Task 2.2: Inventory.tsx + components/inventory/*
- [ ] 列出所有英文字符串（表头、过滤器标签、按钮、空状态、错误消息）
- [ ] 在 `en/inventory.json` 添加 key
- [ ] 改造 `Inventory.tsx` 与 7 个 inventory 组件
- [ ] 运行 typecheck + lint + test:dom
- [ ] Commit

### Task 2.3: Market.tsx + components/market/*
- [ ] 列出英文字符串
- [ ] 在 `en/market.json` 添加 key
- [ ] 改造 Market.tsx 与 components/market/*
- [ ] 运行验收
- [ ] Commit

### Task 2.4: Chests.tsx + ChestsTrackerPanel.tsx + BoxTracker.tsx
- [ ] 列出英文字符串
- [ ] 在 `en/chests.json` 添加 key
- [ ] 改造三个文件
- [ ] 运行验收
- [ ] Commit

### Task 2.5: Loot.tsx + components/loot/*
- [ ] 列出英文字符串（含 ClassifyPromptDialog）
- [ ] 在 `en/loot.json` 添加 key
- [ ] 改造 Loot.tsx 与 5 个 loot 组件
- [ ] 运行验收
- [ ] Commit

### Task 2.6: Lookup.tsx + components/lookup/*
- [ ] 列出英文字符串（13 个 lookup 组件）
- [ ] 在 `en/lookup.json` 添加 key
- [ ] 改造 Lookup.tsx 与 components/lookup/*
- [ ] 运行验收
- [ ] Commit

### Task 2.7: Pets.tsx
- [ ] 列出英文字符串
- [ ] 在 `en/pets.json` 添加 key
- [ ] 改造 Pets.tsx
- [ ] 运行验收
- [ ] Commit

### Task 2.8: LiveMemoryDiagnostics.tsx + LiveMemorySettings.tsx
- [ ] 列出英文字符串
- [ ] 在 `en/liveMemory.json` 添加 key
- [ ] 改造两个文件
- [ ] 运行验收
- [ ] Commit

### Task 2.9: Settings.tsx 剩余文本
- [ ] 列出 Settings.tsx 中除 Language 选择器外的所有英文字符串（CLEAR_ACTIONS、CacheActionRow、各 Section 标题等）
- [ ] 在 `en/settings.json` 补全 key
- [ ] 改造 Settings.tsx
- [ ] 运行验收
- [ ] Commit

### Task 2.10: 通用组件
- [ ] 改造 `WhatsNewModal.tsx`（whatsNew.ts 中的内容也应迁到 i18n）
- [ ] 改造 `SaveStatusBar.tsx`
- [ ] 改造 `AppToolbar.tsx`
- [ ] 改造 `ItemLink.tsx`
- [ ] 改造 `CatalogRefreshButton.tsx`
- [ ] 改造 `LiveReaderIndicator.tsx`
- [ ] 改造 `TrackerConfigRow.tsx`、`TrackerFarmStageSelect.tsx`、`NotificationKindRow.tsx`
- [ ] 改造 `GlobalEntityPanel.tsx`
- [ ] 运行验收
- [ ] Commit

### Task 2.11: appState.ts 对话框 title
- [ ] 修改 `app/src/main/app/appState.ts` 第 316 行 `title: "Choose TBH save file"` 为 `title: t("dialogs:chooseSaveFile")`
- [ ] 修改 filters name `"TBH save"` 为 `t("dialogs:saveFileFilter")`
- [ ] 运行 typecheck + test
- [ ] Commit

### Task 2.12: Phase 2 集成验收
- [ ] `pnpm qa` 全绿
- [ ] 手动走查所有 Tab，切换 en/zh-CN（zh-CN 仍为 `{}`，应回退英文）
- [ ] 确认无遗漏英文硬编码（可用 grep "label: \"" 等模式扫描）
- [ ] Commit

---

## Phase 3：zh-CN 完整翻译

### Task 3.1: 填充 zh-CN/tabs.json
- [ ] 翻译 10 个 Tab 标签（Live→实时、Inventory→物品栏、Chests→宝箱、Loot→掉落、Pets→宠物、Lookup→图鉴、Market→市场、Settings→设置、About→关于、Debug→调试）
- [ ] 启动 dev 验证 Tab 标签切换
- [ ] Commit

### Task 3.2: 填充 zh-CN/tray.json + notifications.json + dialogs.json
- [ ] 翻译托盘菜单 5 项
- [ ] 翻译通知 4 条
- [ ] 翻译对话框 4 项
- [ ] 验证：切换语言后托盘菜单、通知、对话框变中文
- [ ] Commit

### Task 3.3: 填充 zh-CN/about.json + settings.json
- [ ] 翻译 About 页所有 key
- [ ] 翻译 Settings 页所有 key（含 Language 选择器自身）
- [ ] 验证 About / Settings 页中文显示
- [ ] Commit

### Task 3.4: 填充 zh-CN/common.json
- [ ] 翻译通用按钮/状态/错误
- [ ] Commit

### Task 3.5: 填充 zh-CN/live.json
- [ ] 翻译 Live Tab + Overlay 全部 key
- [ ] 验证 Live Tab 与 Overlay 浮窗
- [ ] Commit

### Task 3.6: 填充 zh-CN/inventory.json
- [ ] 翻译 Inventory Tab 全部 key
- [ ] 验证
- [ ] Commit

### Task 3.7: 填充 zh-CN/market.json
- [ ] 翻译 Market Tab
- [ ] 验证
- [ ] Commit

### Task 3.8: 填充 zh-CN/chests.json
- [ ] 翻译 Chests + BoxTracker
- [ ] 验证
- [ ] Commit

### Task 3.9: 填充 zh-CN/loot.json
- [ ] 翻译 Loot Tab + ClassifyPromptDialog
- [ ] 验证
- [ ] Commit

### Task 3.10: 填充 zh-CN/lookup.json
- [ ] 翻译 Lookup Tab
- [ ] 验证
- [ ] Commit

### Task 3.11: 填充 zh-CN/pets.json + liveMemory.json
- [ ] 翻译 Pets + LiveMemory
- [ ] 验证
- [ ] Commit

### Task 3.12: Phase 3 端到端走查
- [ ] 切换到 zh-CN，走查所有 Tab + 托盘 + 通知 + 对话框
- [ ] 修正插值/复数问题
- [ ] `pnpm qa` 全绿
- [ ] Commit

---

## Phase 4：ja / ko 翻译

### Task 4.1: ja 全部 namespace
- [ ] 复制 en/*.json 到 ja/*.json 作为基础
- [ ] 逐 namespace 翻译为日本語
- [ ] 启动 dev，切换到 ja 验证
- [ ] Commit

### Task 4.2: ko 全部 namespace
- [ ] 复制 en/*.json 到 ko/*.json 作为基础
- [ ] 逐 namespace 翻译为 한국어
- [ ] 启动 dev，切换到 ko 验证
- [ ] Commit

### Task 4.3: Phase 4 端到端走查
- [ ] 切换到 ja 走查所有 Tab + 托盘 + 通知 + 对话框
- [ ] 切换到 ko 走查
- [ ] 修正插值问题
- [ ] `pnpm qa` 全绿
- [ ] Commit

---

## Phase 5：QA 与上线

### Task 5.1: 完整 QA
- [ ] `pnpm qa` 全绿
- [ ] `pnpm qa:dev`（如 UI 可见）
- [ ] 手动 4 语言全走查

### Task 5.2: 文档同步
- [ ] 在 `docs/ARCHITECTURE.md` 追加 i18n 段落
- [ ] 在 `docs/agent/CHANGELOG-RELEASE.md` 添加变更条目
- [ ] 在 `docs/agent/layers/` 必要时补充 i18n 约定
- [ ] bump version：`app/package.json` 1.18.0 → 1.19.0
- [ ] Commit

### Task 5.3: PR 准备
- [ ] 按 `docs/agent/PULL-REQUEST.md` 写 PR 描述
- [ ] 截图（可选）：4 语言下 Settings + About + Live 对比
- [ ] 提交 PR

---

## Self-Review 检查清单

写完计划后对照 spec 自检：

1. **Spec 覆盖**：
   - ✅ Section 1 目标 → Phase 1-2 全覆盖
   - ✅ Section 2 架构 → Task 1.2-1.4, 1.6, 1.11
   - ✅ Section 3 主进程集成 → Task 1.6-1.10
   - ✅ Section 4 渲染进程集成 → Task 1.11-1.15
   - ✅ Section 5 翻译 Key 约定 → Task 1.3 JSON 文件已用嵌套 + 插值
   - ✅ Section 6 分阶段交付 → Phase 1-5 完全对应
   - ✅ Section 7 测试策略 → Task 1.2/1.4/1.5/1.6/1.8/1.10/1.11 都有测试
   - ✅ Section 8 风险缓解 → 工厂懒初始化（Task 1.6）解决 whenReady 前访问；跨窗口同步 MVP 接受下次重启
   - ✅ Section 9 依赖检查 → Task 1.1
   - ✅ Section 10 完成定义 → Phase 5
   - ✅ Section 11 ADR → 计划中已体现（D1-D6）

2. **占位符扫描**：Phase 2-5 的任务用了「列出英文字符串」这种描述性步骤，因为完整列出会超出计划文档合理长度。这是合理的简化，执行时按现有模式即可。Phase 1 的所有代码步骤都有完整代码。

3. **类型一致性**：
   - `AppLanguage` 在 shared/language.ts 定义，被 types.ts、config.ts、main/i18n.ts、renderer/i18n.ts 使用 — 一致
   - `ResolvedLanguage` 在 core/i18n/factory.ts 定义，被 main/i18n.ts、renderer/i18n.ts 使用 — 一致
   - `t(key, opts?)` 签名在 main/i18n.ts、NotificationService、trayService 一致
   - `initMainI18n(config)`、`initRendererI18n(language)`、`changeLanguage`/`changeRendererLanguage` 命名一致
