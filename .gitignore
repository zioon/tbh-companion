# Python (legacy reference tool)
.venv/
__pycache__/
*.pyc

# Node / Electron (companion app)
node_modules/
app/dist/
app/out/
app/release/
app/.electron-cache/
app/storybook-static/
app/bench-results.json
app/*-bench.json
# Legacy npm lockfile (project uses pnpm)
app/package-lock.json
# Generated price snapshots — built by CI (lookup-prices workflow) and
# seed-market-prices.mjs; clients download fresh from GitHub releases
app/prices.json
app/prices.*.json
app/lookup_prices.json
*.log

# Runtime output
logs/

# Personal save dumps - never commit your own save data
sample/
*.es3
*.decrypted.json

# Game locale dump — now a bundled data file (shipped via extraResources,
# same as gamedata.json). Regenerated by scripts/dump_game_locale.py
# when game locale tables change; commit the result so CI builds ship it.

# OS / editor
Thumbs.db
.DS_Store
.agents/

# Specs / planning artifacts — local only
.specs/

# Research working notes — local only
_research/

# Draft docs — intentionally untracked until reviewed
docs/reviews/
docs/business/
docs/design/branding.md
docs/design/icons/concept-sidehero-512.png
docs/showcase/

# POC
poc/

# Worktrees
worktrees/