name: CI

on:
  pull_request:
    branches: [main]
    paths:
      - "app/**"
      - ".github/workflows/**"
      - "data/**"
      - "docs/agent/**"
      - "docs/flow-viz/**"
      - "docs/BUSINESS-FLOWS.md"
      - ".cursor/skills/**"
      - ".claude/skills/**"
      - "scripts/sync-agent-skills.mjs"
      - "scripts/sync-agent-docs.mjs"
  push:
    branches: [main]
    paths:
      - "app/**"
      - ".github/workflows/**"
      - "data/**"
      - "docs/agent/**"
      - "docs/flow-viz/**"
      - "docs/BUSINESS-FLOWS.md"
      - ".cursor/skills/**"
      - ".claude/skills/**"
      - "scripts/sync-agent-skills.mjs"
      - "scripts/sync-agent-docs.mjs"

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  qa:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: app
    steps:
      - uses: actions/checkout@v5

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: pnpm
          cache-dependency-path: app/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify agent skills mirror
        working-directory: .
        run: node scripts/sync-agent-skills.mjs --check

      - name: Verify generated agent docs
        working-directory: .
        run: node scripts/sync-agent-docs.mjs --check

      - name: Verify flow-viz data
        working-directory: .
        run: node docs/agent/scripts/build-flow-viz.mjs --check

      - name: Automated QA gate
        run: pnpm qa

      - name: pnpm audit (high severity)
        run: pnpm audit --audit-level high
        continue-on-error: true
