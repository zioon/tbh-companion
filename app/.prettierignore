node_modules/
out/
release/
dist/
storybook-static/
pnpm-lock.yaml
*.tgz
# 测试/运行期产物：trackingService 测试会写入共享的 app/record_log.json
# （已在 .gitignore 中忽略），prettier 不应校验它。
record_log.json
# QA 交付物：QA 独立回归报告（agent 生成的 Markdown，非源码，不应参与格式门禁）
QA_REPORT_*.md
