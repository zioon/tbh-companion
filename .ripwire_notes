# ripwire field notes v1 — one per line: <canonical-id or path> <TAB> <ISO-date> <TAB> <text> [<TAB> <HEAD sha> <TAB> <branch>]. Kept SORTED (merge-friendly union); dates are git committer-clock, not wall time; the trailing sha/branch pair is present only on provenance-stamped notes.
.github/workflows/release.yml	2026-09-24	website/data/release.json 由本 workflow 的 manifest 步骤写入并提交，不是死文件 —— 删掉它等于改发版流水线。	3ff93e3a6e6551d33424aae4f24a7714c53ef85f	main
acquireScanBuffer	2026-09-24	区域遍历必须复用每进程扫描缓冲（core 走 readChunk、main 走 acquireScanBuffer），不要按分块新分配 4MiB Buffer：BufferPool 按精确字节分桶，~20 个区域即填满后 release() 全丢。2026-09-24 实测：修复前 RSS +18MB/min 持续攀升，修复后零增长。	3ff93e3a6e6551d33424aae4f24a7714c53ef85f	main
app/src/core/wish/coinGroups.ts	2026-09-24	排序契约 = count 降序优先，再按用户语言 localeCompare；同一行为在 5 处实现（coinGroups/coinOverrides/wishTracker/boxOpenTracker/chestDropTracker），不要为过测试改实现。测试期望不要用无 locale 参数的 localeCompare 构造（本机 zh-CN 按拼音、CI en-US 按码位 → 本机假绿、CI 必红）。	3ff93e3a6e6551d33424aae4f24a7714c53ef85f	main
boxOpenHealWouldHelp	2026-09-24	该 getter 是 box-open 事件重跑 extractor 的唯一闸门（只看 missingBoxOpenFields）。不要退回 enrichmentComplete —— player.boxData 三件套在 ES3 字节流层不可派生，退回会让每次 attach 白跑 47.7s（107MB GA 扫描）。	3ff93e3a6e6551d33424aae4f24a7714c53ef85f	main
docs/DATA-UPDATE.md	2026-09-24	富数据（图标/stats/掉落来源/合成/掉率/本地化转储）不会自动更新，6 个 workflow 都重建不了 data/；游戏版本变更后必须在本机跑管道2并随发版发布。基础目录（gamedata/locale）由 CatalogRefreshService 自动重提取。	3ff93e3a6e6551d33424aae4f24a7714c53ef85f	main
docs/SAVE_FORMAT.md	2026-09-24	分析存档纪律：①先复制固定副本（SaveFile_Live.es3 会被游戏持续改写）②UniqueId/ItemUniqueId 超出 MAX_SAFE_INTEGER，JSON.parse 会舍入并把不同材料并键、求和全错 → 必须走无损字符串正则 ③数值结论必须标注「副本/时刻/解析路径」。槽位堆叠(v1.2.6+)：Quantity 每格上限 5，Quantity:0 为空槽，同一 UID 可被多格复用（聚合不能按 UID 去重）。	3ff93e3a6e6551d33424aae4f24a7714c53ef85f	main
docs/agent/GIT.md	2026-09-24	本工作区常年有并行会话改同一棵树 → 提交只用显式路径 git add -- <files>，禁止 git add <目录>/-A/commit -a；禁止 git rebase（实测会清空工作区，用 git merge <sha>）；push 必须用内联凭据 URL。	3ff93e3a6e6551d33424aae4f24a7714c53ef85f	main
lazyFunc	2026-09-24	禁止在任何调用点内声明 koffi 函数（kernel32().func(...)）；每次声明泄漏原生内存 ≈176B/次（50 万次调用 +83.8MB，约 1GB/小时）。必须经 lazyFunc() 每进程只声明一次。	3ff93e3a6e6551d33424aae4f24a7714c53ef85f	main
